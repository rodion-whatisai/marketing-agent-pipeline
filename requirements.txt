# Стадия 01 (сканер) — рабочий код в 01-client-discovery/engine/
playwright>=1.40
requests>=2.31
anthropic>=0.39
colorama>=0.4        # цвета в терминале (логи с уровнями), кроссплатформенно

# Стадия 01 — pitch-adviser: чтение CPM-бенчмарков из Google Sheets
# (сервисный аккаунт, ключ в pitch-adviser/.secrets/gsheets.json — вне git)
gspread>=6.0
google-auth>=2.0

# Тесты
pytest>=8.0

# Примечание: 03-execution/engine — концептуальный движок, чистый stdlib (dataclasses, json),
# отдельных зависимостей не требует.
